# 🚀 v2.0 升级执行步骤

## 📋 准备工作检查清单

- [x] 已分析两个版本的差异
- [x] 已创建升级策略文档
- [x] 已创建 CHANGELOG.md
- [x] 已创建升级指南
- [ ] 准备执行文件操作

## 🎯 执行步骤（按顺序）

### 第一阶段：文件备份和迁移

#### 步骤 1：创建 legacy 文件夹
```bash
mkdir legacy
```

#### 步骤 2：备份 v1.0 文件
```bash
# 移动主程序和打包脚本到 legacy
move autopaste.py legacy/
move build.py legacy/
move test_imports.py legacy/
move 密码自动输入工具.spec legacy/
```

#### 步骤 3：创建 legacy 说明文档
在 `legacy/README.md` 中说明这是 v1.0 备份。

#### 步骤 4：复制 v2.0 文件
```bash
# 从 autokey-claude4-5 复制到主目录
copy ..\autokey-claude4-5\autopaste.py .
copy ..\autokey-claude4-5\build.py .
```

### 第二阶段：文档更新

#### 步骤 5：更新 README.md
添加以下内容：
- v2.0 新特性说明
- Unicode 支持说明
- 版本历史链接
- 热键变更提示

#### 步骤 6：更新 RELEASE_NOTES.txt
创建 v2.0 的发布说明，包含：
- 主要改进
- 新增特性
- 破坏性变更
- 迁移指南

#### 步骤 7：更新其他文档
- 更新 `下一步操作.md`
- 更新 `命令清单.txt`
- 更新 `项目总结.md`

### 第三阶段：Git 提交

#### 步骤 8：查看更改
```bash
git status
git diff
```

#### 步骤 9：添加所有更改
```bash
git add .
```

#### 步骤 10：提交更改
```bash
git commit -m "Upgrade to v2.0.0: Unicode support and improved architecture

Major changes:
- Add Unicode/Chinese character support
- Migrate to pure Windows API (ctypes + SendInput)
- Add runtime configuration menu
- Reduce dependencies from 5 to 2
- Change hotkey from Ctrl+Alt+J to Ctrl+Alt+P
- Improve error handling and progress display
- Backup v1.0 to legacy/ folder

Breaking changes:
- Hotkey changed to Ctrl+Alt+P
- API implementation completely rewritten

See CHANGELOG.md for full details."
```

#### 步骤 11：推送到 GitHub
```bash
git push origin main
```

### 第四阶段：Release 发布

#### 步骤 12：打包 v2.0
```bash
python build.py
```

#### 步骤 13：创建 GitHub Release
1. 访问：https://github.com/buptanswer/autopaste/releases/new
2. 填写信息：
   - **Tag**: `v2.0.0`
   - **Title**: `AutoPaste v2.0.0 - Unicode 支持和架构升级`
   - **Description**: 复制更新后的 RELEASE_NOTES.txt
3. 上传文件：
   - `dist/密码自动输入工具.exe`（v2.0）
   - 可选：同时提供 v1.0 的 exe（从 legacy 打包）

#### 步骤 14：更新仓库设置
- 确保 Release 可见
- 更新仓库描述（如果需要）
- 添加新的 Topics（如 unicode, multilingual）

## 📝 需要创建/更新的文件清单

### 新建文件
- [x] `CHANGELOG.md` - 版本变更记录
- [x] `升级到v2.0指南.md` - 升级指南
- [x] `v2.0升级执行步骤.md` - 本文件
- [ ] `legacy/README.md` - v1.0 备份说明

### 更新文件
- [ ] `README.md` - 添加 v2.0 特性
- [ ] `RELEASE_NOTES.txt` - v2.0 发布说明
- [ ] `下一步操作.md` - 更新为 v2.0 步骤
- [ ] `命令清单.txt` - 更新热键说明
- [ ] `项目总结.md` - 更新版本信息

### 移动文件
- [ ] `autopaste.py` → `legacy/autopaste.py`
- [ ] `build.py` → `legacy/build.py`
- [ ] `test_imports.py` → `legacy/test_imports.py`
- [ ] `密码自动输入工具.spec` → `legacy/密码自动输入工具.spec`

### 复制文件
- [ ] `../autokey-claude4-5/autopaste.py` → `./autopaste.py`
- [ ] `../autokey-claude4-5/build.py` → `./build.py`

## ⚠️ 注意事项

### 执行前确认
1. ✅ 当前工作目录是项目根目录
2. ✅ 已经完成 v1.0 的初始提交
3. ✅ 有 `../autokey-claude4-5/` 文件夹
4. ✅ Git 状态干净（没有未提交的更改）

### 执行中注意
1. 每个步骤执行后检查结果
2. 如果出错，立即停止并检查
3. 保持 Git 提交的原子性

### 执行后验证
1. 检查文件结构是否正确
2. 测试 v2.0 是否能正常运行
3. 确认 Git 历史清晰
4. 验证 GitHub 上的显示

## 🔄 回滚方案

如果升级出现问题，可以回滚：

```bash
# 方案 1：Git 回滚
git reset --hard HEAD~1

# 方案 2：手动恢复
copy legacy\autopaste.py .
copy legacy\build.py .
```

## 📞 需要帮助？

如果执行过程中遇到问题：
1. 查看错误信息
2. 检查文件路径是否正确
3. 确认 Git 状态
4. 参考 `升级到v2.0指南.md`

---

**准备执行时间**: 2025-11-27  
**预计执行时间**: 10-15 分钟  
**风险等级**: 低（有完整备份）